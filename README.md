# roryhunter/docker-compose-node

This is a docker image based on the official `node` image that also
contains `docker` and `docker-compose`. This is to simplify some
development and testing workflows.
